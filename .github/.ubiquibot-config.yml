plugins:
  issues.labeled:
    - uses:
        - plugin: ubiquibot/user-activity-watcher:compute.yml@pull/1
          type: github
          with:
            unassignUserThreshold: 5
            sendRemindersThreshold: 1
